services:
  mysqldb:
    image: mysql:latest
    container_name: mysql_container
    environment:
      MYSQL_ROOT_PASSWORD: 'mysecretpassword'
      MYSQL_DATABASE: user_db
    ports:
      - "3306:3306"
    restart: always